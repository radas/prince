package cz.tieto.princegame.domain;

import static org.junit.Assert.*;

import java.util.HashMap;
import java.util.Map;

import org.junit.Test;

import cz.tieto.princegame.client.gameobject.ObstacleImpl;
import cz.tieto.princegame.common.gameobject.Obstacle;

public class KnightTest {
/*
	@Test
	public void testIsKnightDead() {
		Map<String, String> map = new HashMap<String,String>();
		
		map.put("dead", "true");
		Obstacle obstacle = new ObstacleImpl(Knight.KNIGHT_NAME, 1, map);

		boolean isDead = Knight.isKnightDead(obstacle);
		assertTrue(isDead);
	}

	@Test
	public void testIsNotKnightDead() {
		Map<String, String> map = new HashMap<String,String>();
		map.put("dead", "false");
		Obstacle obstacle = new ObstacleImpl(Knight.KNIGHT_NAME, 1, map);

		boolean isDead = Knight.isKnightDead(obstacle);
		assertFalse(isDead);
	}

	@Test
	public void testNotPropertyKnightDead() {
		Map<String, String> map = new HashMap<String,String>();
		Obstacle obstacle = new ObstacleImpl(Knight.KNIGHT_NAME, 1, map);
		boolean isDead = Knight.isKnightDead(obstacle);
		assertFalse(isDead);
	}
    */
}
